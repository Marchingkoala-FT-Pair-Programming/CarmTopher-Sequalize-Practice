const express = require("express");
const router = express.Router();

// this page will be filled with what's not in the main index.js file
// app.get files will be transported here & changed to router.get()

module.exports = router
